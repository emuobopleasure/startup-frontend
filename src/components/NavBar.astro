<header
    class="header text-white bg-[#111] p-[1.25rem] z-10 fixed top-0 w-full lg:w-full xl:px-[10.2rem] 2xl:px-[18.2rem] lg:py-[1.5rem] shadow-[#0e0e0e] shadow-md"
>
    <div class="nav-container max-w-[60rem] mx-auto text-white">
        <nav class="">
            <div class="nav-div flex justify-between items-center">
                <h1
                    class="leading-[-0.03em] text-[1.05rem] lg:text-[1.2rem] font-bold"
                >
                    <a href="/"> Recursive Zero</a>
                </h1>
                <ul
                    class="menu-list flex items-center lg:flex-row-reverse gap-[1.5rem]"
                >
                    <li>
                        <button>
                            <i class="fa fa-sun-o" aria-hidden="true"></i>
                        </button>
                    </li>
                    <li id="menuToggle" class="lg:hidden menu-button">
                        <button id="closeBtn" class="hidden">
                            <i class="fa fa-times" aria-hidden="true"></i>
                        </button>
                        <button id="openBtn">
                            <i class="fa fa-bars" aria-hidden="true"></i>
                        </button>
                    </li>
                    <!-- desktop menu -->
                    <div
                        class="hidden lg:flex items-center gap-[0.7rem] text-[16px] leading-[25.6px]"
                    >
                        <a href="">
                            <button
                                class="bg-[#222] px-[10px] py-[5px] rounded-md"
                            >
                                Start Innovating Now
                            </button>
                        </a>
                        <a href="">
                            <button
                                class="bg-[#FFCC00] px-[10px] py-[5px] rounded-md text-[#111]"
                            >
                                Start Saving Time!
                            </button>
                        </a>
                    </div>
                </ul>
            </div>
            <!-- mobile nav-overlay -->
            <div
                id="menu-open"
                class="nav-overlay hidden pt-[20px] h-dvh lg:hidden"
            >
                <div class="pt-[10px]">
                    <div
                        class="py-[20px] flex gap-[20px] items-center justify-between text-[14px] leading-[22.4px] md:text-[16px] md:leading-[25.6px]"
                    >
                        <a href="" class="w-full">
                            <button
                                class="bg-[#222] py-[5px] px-[10px] h-[35px] w-full rounded-md"
                            >
                                Start Innovating Now
                            </button>
                        </a>
                        <a href="" class="w-full">
                            <button
                                class="bg-[#FFCC00] py-[5px] px-[10px] h-[35px] w-full rounded-md text-[#111]"
                            >
                                Start Saving Time!
                            </button>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </div>
</header>

<script>
    const menuToggle = document.querySelector("#menuToggle");
    
    menuToggle.addEventListener("click", () => {
        const showMenu = document.querySelector("#menu-open");
        const openBtn = document.querySelector('#openBtn')
        const closeBtn = document.querySelector('#closeBtn')

        if (showMenu.style.display === "block") {
            showMenu.style.display = "none"
            closeBtn.style.display = 'none'
            openBtn.style.display = 'inline'
        } else {
            showMenu.style.display = "block"
            closeBtn.style.display = 'inline'
            openBtn.style.display = 'none'
        }
    });
</script>

<style>
    .nav-container {
        /* padding: 0.5rem 0;
        color: white;
        z-index: 10;
        position: fixed;
        width: 90%;
        background-color: #111;
        height: 4rem; */
    }

    /* @media screen and (min-width: 992px) {
        .nav-container {
            width: 99vw;
            padding: 0 4rem;
        }
    } */

    /* header {
        display: flex;
        justify-content: center;
        box-shadow: 0px 10px 5px 0px yellow;
    } */

    header {
        /* outline: 1px solid red; */
    }

    nav {
        /* display: flex;
        justify-content: center; */
    }

    /* .nav-div {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    @media screen and (min-width: 992px) {
        .nav-div {
            padding: 0 12rem;
        }
    }

    .nav-div ul {
        list-style-type: none;
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .nav-div button {
        all: unset;
    } */

    /* .menu-list {
        display: flex;
        justify-content: space-between;
        align-items: center;
    } */

    /* h1 {
        font-weight: 100;
        font-size: 1rem;
        letter-spacing: -0.03em;
    }

    h1 a {
        all: unset;
    } */
</style>
