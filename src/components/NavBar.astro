<header class="header text-white bg-[#111] py-[1.5rem] z-10 fixed top-0 w-full px-[1.2rem] lg:w-full lg:px-[12rem] lg:py-[1.5rem] shadow-[#000000] shadow-md">
    <div
        class="nav-container text-white">
        <nav class="">
            <div class="nav-div flex justify-between items-center">
                <h1 class="leading-[-0.03em] text-[1.05rem] lg:text-[1.2rem] font-bold">
                    <a href="/"> Recursive Zero</a>
                </h1>
                <ul class="menu-list flex items-center lg:flex-row-reverse gap-[2rem]">
                    <li>
                        <button>
                            <i class="fa fa-sun-o" aria-hidden="true"></i>
                        </button>
                    </li>
                    <li class="lg:hidden">
                        <button>
                            <i class="fa fa-bars" aria-hidden="true"></i>
                        </button>
                    </li>
                    <!-- desktop menu -->
                    <div class="hidden lg:flex items-center gap-[0.7rem] text-[16px] leading-[25.6px]">
                        <a href="">
                            <button class="bg-[#222] px-[10px] py-[5px] rounded-md">
                                Start Innovating Now
                            </button>
                        </a>
                        <a href="">
                            <button class="bg-[#FFCC00] px-[10px] py-[5px] rounded-md text-[#111]">
                                Start Saving Time!
                            </button>
                        </a>
                    </div>
                </ul>
            </div>
        </nav>
    </div>
</header>

<style>
    .nav-container {
        /* padding: 0.5rem 0;
        color: white;
        z-index: 10;
        position: fixed;
        width: 90%;
        background-color: #111;
        height: 4rem; */
    }

    /* @media screen and (min-width: 992px) {
        .nav-container {
            width: 99vw;
            padding: 0 4rem;
        }
    } */

    /* header {
        display: flex;
        justify-content: center;
        box-shadow: 0px 10px 5px 0px yellow;
    } */

    header {
        /* outline: 1px solid red; */
    }

    nav {
        /* display: flex;
        justify-content: center; */
    }

    /* .nav-div {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    @media screen and (min-width: 992px) {
        .nav-div {
            padding: 0 12rem;
        }
    }

    .nav-div ul {
        list-style-type: none;
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .nav-div button {
        all: unset;
    } */

    /* .menu-list {
        display: flex;
        justify-content: space-between;
        align-items: center;
    } */

    /* h1 {
        font-weight: 100;
        font-size: 1rem;
        letter-spacing: -0.03em;
    }

    h1 a {
        all: unset;
    } */
</style>
